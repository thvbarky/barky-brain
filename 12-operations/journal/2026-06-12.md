# Journal — 12 juin 2026

## 🎯 Décisions actées

_Aucune nouvelle décision ce jour — les 4 bloqueurs pré-relance restent en attente d'exécution par Thomas._

## 💡 Insights techniques

- **API Meta + time_range sur campagne PAUSED** : quand une campagne est pausée, l'API renvoie `"Not available"` pour toutes les métriques sur la période pausée. Utiliser `date_preset: maximum` pour récupérer les cumuls depuis le début. Ne pas confondre "Not available" avec une erreur — c'est simplement 0 dépense.
- **Shopify Analytics** vs **Shopify Orders** : `run-analytics-query FROM sessions` donne le funnel complet (sessions, ATC, checkout, commandes) par jour. `list-orders` donne les commandes réelles avec détail client. Les deux sont complémentaires — Sessions pour le funnel, Orders pour la réalité commerciale.
- **J9 FINAL confirmé** = 9 sessions (identique au run 18h). Le run 3×/jour est fiable pour les données Shopify (pas de décalage important sur les sessions directes).

## Insights produit

- **0 commande confirmé** depuis le lancement (seule order Shopify = #1001 test Thomas à 0,00€). Les 2 ATC + 2 checkouts de J8 (10/06) n'ont pas converti → friction checkout à résoudre.
- **Ad1 PDP cumul API = 44,60€** · 14 486 impr · CTR 2,49% · CPC 0,12€. C'est la créa qui a absorbé 38% du budget total (116,98€) et qui est la seule à avoir généré 1 ATC social. Signal fort : l'angle "c'est l'âge reframe" + destination PDP directe est le couple gagnant.

## 🔄 Tâches en attente

1. **Thomas → exclure IG** sur Ad2 `signes-mobilite-ete` (Manager Ads → Ad set → Placements → manual → décocher Instagram)
2. **Thomas → exclure IG** sur Ad3 LP `5-problemes-1-formule · LP` (même procédure)
3. **Thomas → clarifier** `Acquisition Test #1` (120248349620690732) : archiver ou garder pour phase 2 ?
4. **Thomas → relancer** la campagne principale après les 3 actions ci-dessus
5. **Thomas → investiguer** abandoned checkouts Shopify admin (2 checkouts J8 non convertis)
