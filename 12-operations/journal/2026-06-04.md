# Journal — jeudi 4 juin 2026

> Suite directe du 03/06. Brainstorming plan 3 jours (mode `/brainstorming`, 80/20) + vérif funnel/pixel complète + faux incident « site down ».

## 🎯 Décisions actées

- **Plan 3 jours « signal ads » acté** (Approche C — funnel d'abord, puis signal). Nord : trancher quel **angle + destination (LP vs PDP)** convertit le mieux. Contraintes : 3-4 h/j, organique zappé, CBO 25 €/j. Feuille de route + scorecard : [`12-operations/2026-06-04-plan-3jours-signal-ads.md`](../2026-06-04-plan-3jours-signal-ads.md).
- **Duel LP vs PDP jugé sur le coût par `AddToCart`** (pas ViewContent — la LP ne le fire pas).
- **Ad8 Video races-poids → pause recommandée (urgence)** : J2 (04/06) Ad8 + Copie ont absorbé 28,60 € / 96% du budget CBO. CTR 12-15% sur Facebook = taps fantômes Reels. Coût/session J2 = 3,28 € vs 0,25 € J1. Appels MCP prêts dans `08-ads/dashboard/DASHBOARD.md` — Thomas exécute dans Ads Manager. IDs : `120248464885400732` (LP) + `120248464880070732` (PDP Copie).
- **Ad3 Typo 5-problemes-1-formule LP = signal de référence** : seule créa ≥1 000 impr propres (2 840 impr). CTR Instagram 2,43% @ CPC 0,14 € = sain. Angle « 5 problèmes / 1 formule » à développer en priorité une fois Ad8 éteint.
- **LP : 4 fuites conversion corrigées + déployées** (gut-check de `multivitamin-v2.html`). (#1) bloc hero d'action sous le titre = CTA + offre above-the-fold avant le long advertorial ; (#2) CTA #1 « Vérifier la disponibilité » → « Découvrir l'offre de lancement » / « Commencer le rituel » ; (#3) accroche symptômes (pelage/articulations/digestion/coup de vieux) pour scent-match avec les pubs ; (#4) prix/offre (−32 % · 25,50 €/mois · 0,85 €/j · garantie 60j) dès le hero. Déployé via `shopify_deploy_landing.py` (pageUpdate OK, vérifié API : nouvelles chaînes présentes, ancien CTA = 0). **CTA → PDP gardé** (produit = choix abo + posologie poids ; ATC direct écarté). Coïncide avec la coupe d'Ad8 → bras LP/PDP repartent propres ensemble.

## 💡 Insights techniques

- **Vérif pixel = funnel blindé.** Un seul pixel `1053208107278240` sur LP + PDP. LP : `PageView` (méthode **Manuel**, posé dans le `<head>` du template → contourne le strip `{% layout none %}`). PDP : `PageView` + `ViewContent` (app Shopify). `AddToCart` OK avec params (`value`, `currency`, `content_ids`). Le « piège fatal » (LP non trackée) **n'était pas armé** — mon alerte initiale était un faux positif de géo-blocage.
- **NE PAS tester le storefront Barky via curl serveur** : le compte vend uniquement en France (Shopify Markets) → tout egress hors FR reçoit « this store isn't available in your country » + 404 erratiques. Vérifs storefront/pixel = côté navigateur Thomas.
- **Faux « site down » = réseau local Thomas** : « marche en 4G pas en WiFi » = cache DNS / VPN côté box, PAS panne Shopify. DNS de fond OK (`barky.pet → 23.227.38.65`, confirmé 1.1.1.1 + 8.8.8.8). `/admin` 403 sur curl non authentifié = normal. Fix : DNS public, flush `dscacheutil`, reboot box, couper VPN.
- **Watch-item : `AddToCart` en méthode Manuel** → vérifier pas de double-fire (manuel + app Shopify) qui gonflerait les ATC.
- **Tools Meta gated sur le compte** : `ads_get_datasets/_stats`, `ads_get_creatives`, `ads_get_ad_preview` → reads créa/dataset via Thomas/Ads Manager, pas via MCP.
- **Écart « vues de page » Meta (~350) vs sessions Shopify (14) = NORMAL et attendu, pas un bug.** Deux causes cumulées : (1) le pixel Meta est en **Manuel dans le `<head>`** → fire `PageView` instantanément au début du parsing, alors que l'analytics Shopify (web-pixels-manager) se charge **plus tard** dans le cycle de vie → un clic mobile qui rebondit en <1 s compte côté Meta mais jamais côté Shopify ; (2) **clics poubelle** : aujourd'hui CTR **12,4 % @ CPC 0,03 €** sur placement **Facebook** (529 link clicks) vs **2,4 %** sur Instagram (sain) → quasi tous des taps accidentels (probable Reels/feed mobile). Funnel réel 04/06 : 7 979 impr → 534 link clicks → ~350 vues page Meta → **14 sessions Shopify**. **Règle : se fier aux sessions Shopify, jamais aux « vues de page » Meta comme dénominateur de conversion.**
- **Ne pas vérifier le pixel par `curl` serveur** (rappel renforcé) : egress hors FR = page géo-bloquée/dégradée, le grep `fbq` peut renvoyer 0 à tort. Preuve fiable que Shopify track = l'API Analytics renvoie des sessions avec source `facebook`, pas le HTML curlé.
- **À surveiller — placement Facebook à 12 % CTR** : isoler la *position* (Feed vs Reels) ; si Reels crache le CTR pourri, l'exclure ou le sortir en ad set séparé. ~28 € brûlés le 04/06 surtout en clics fantômes.
- **CBO aveugle sur CTR fantôme — pattern confirmé J1→J2** : Ad8 Video 0,56 €/CTR 15,79% FB (J1) → promu à 28,60 € / 96% budget (J2). Coût/session ×13. Règle : sans event aval (ATC/achat) comme objectif, le CBO récompense mécaniquement le faux CTR vidéo. Seul remède = pause la créa coupable.
- **Vidéo portrait TikTok réutilisée sur Meta Reels = toujours fantôme** : tap pour lancer la vidéo = link_click comptabilisé. Ne jamais servir un format portrait sur Facebook sans isolation par placement (Instagram uniquement) ou objectif ATC.

## Insights produit / marché

- *(RAS aujourd'hui — journée infra/tracking.)*

## 🔄 Tâches en attente

- **URGENT (aujourd'hui / demain matin)** : Thomas met Ad8 Video en pause dans Ads Manager (2 IDs : `120248464885400732` + `120248464880070732`). Détail + appels MCP dans `08-ads/dashboard/DASHBOARD.md §Priorité 1`.
- **Jour 3 (05/06)** : vérifier que le CBO a réalloué vers Ad3/Ad1/Ad2. Pull CTR Instagram par créa. Chercher le 1er ATC Shopify. Si toujours 0 ATC après réallocation — revoir la landing page.
- **Jour 4 (06/06)** : lecture consolidée angle × destination (si ≥1 000 impr propres sur Ad1 + Ad2), concentrer le budget sur le meilleur angle, activer retargeting -25% si ≥500 visiteurs cumulés.
- Déduplifier `AddToCart` si ATC anormalement nombreux une fois le premier event observé.
