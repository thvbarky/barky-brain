# Journal — Samedi 13 juin 2026

## 🎯 Décisions actées

*(aucune décision actée — run routinier 8h + 12h · rien de nouveau · 4 bloqueurs inchangés)*

**Run 12h** : J11 partiel = **6 sessions direct / 0 ATC / 0 commande**. Campagne toujours PAUSED ~188h. Aucun signal nouveau.

## 💡 Insights techniques

- **J10 FINAL = 34 sessions** (corrigé depuis 12 loguées à ~20h) : les runs à 18h/20h ne capturent pas les sessions nocturnes. La règle FINAL = confirmation API du lendemain matin est la seule approche fiable. Même volume que J8 (34 sessions aussi).
- **API Meta `amount_spent` returns "Not available"** quand il n'y a pas de spend sur la période demandée (campaign PAUSED). Les champs `effective_status` et `delivery` sont toujours disponibles — suffisent pour confirmer le statut pause.
- **Champs Meta invalides** au niveau campaign : `inline_link_clicks`, `inline_link_click_ctr`, `landing_page_views`. Utiliser à la place : `actions:link_click`, `cost_per_link_click`. Champs valides confirmés : `amount_spent`, `impressions`, `reach`, `frequency`, `clicks`, `ctr`, `cpc`, `cpm`, `effective_status`, `delivery`.

## Insights produit

*(aucun nouveau ce run)*

## 🔄 Tâches en attente

- **URGENT** — 4 bloqueurs relance ads (inchangés depuis J5 07/06) :
  1. Clarifier `Acquisition Test #1` (archiver ou planifier)
  2. Exclure IG sur `Ad2 · ProductHero · signes-mobilite-ete`
  3. Exclure IG sur `Ad3 · Typo · 5-problemes-1-formule · LP`
  4. Réactiver la campagne principale
- Investiguer friction checkout (3 abandons / 3 tentatives : J8 × 2 + J10 × 1) → Shopify admin → Orders → Abandoned checkouts
