# Journal — mardi 5 mai 2026

## 🎯 Décisions actées

- **Refonte `BARKY_CERVEAU.md`** (décision #15 dans `decisions.md`) : passage de **68 336 → 22 288 chars** (-67%), sous le seuil performance Claude Code (40k chars). Inversion du flux de vérité : le master devient **synthèse stratégique always-loaded** + **index navigable**, les fichiers thématiques deviennent la source de vérité opérationnelle. Zéro perte d'info — tout le détail (marché, concurrents, personas, SKU, claims, packaging, unit eco, private label, pitch deck, validation, acquisition, roadmap, Moubeche, Felis) déjà présent ou plus riche dans les thématiques.
- **Profil fondateur corrigé partout** : suppression des mentions erronées *"étudiant Dauphine + M&A"* dans 5 fichiers (master + `12-operations/validation-6sem.md` + `07-content/strategy.md` + `08-ads/workflow-creative-mcp.md` + `14-knowledge/playbook-moubeche.md`). Source unique du profil désormais : [`01-identite/THOMAS_PROFIL_1.md`](../../01-identite/THOMAS_PROFIL_1.md). Tous les fichiers qui invoquent le contexte fondateur pointent vers ce fichier.

## 💡 Insights techniques

- **Le warning *"Large BARKY_CERVEAU.md will impact performance (65.7k chars > 40.0k)"*** est purement une alerte de tokens consommés à chaque conversation, **pas une troncation** — Claude lit toujours le fichier en entier. Le coût réel : ~20% de la fenêtre de contexte mangée avant la première question. Modulariser (master = synthèse + liens, thématiques = détail) est la bonne réponse.
- **Sur-duplication détectée par audit** : avant la refonte, 80% du contenu du master existait déjà 1:1 dans les fichiers thématiques (qui s'auto-déclaraient comme *"extraits opérationnels"* du master). Pour certains sujets (concurrents, personas), les thématiques étaient **plus riches** que le master. La refonte n'a donc rien supprimé — uniquement nettoyé la duplication.
- **Workflow de refonte safe** : (1) audit en parallèle des thématiques pour valider que tout est bien là · (2) rédaction dans un fichier `.new.md` temporaire · (3) `mv` atomique · (4) `grep` post-swap sur 16 concepts clés pour vérifier qu'aucune info n'a disparu du repo. Pattern réutilisable pour les futurs nettoyages.

## Insights produit / marché

- *(Pas d'insight produit/marché aujourd'hui — session focalisée sur l'infrastructure du repo.)*

## 🔄 Tâches en attente

- [ ] **Surveiller le cron 17h** : vérifier que la routine quotidienne d'enrichissement comprend bien la nouvelle structure (synthèse stratégique dans le master, détail dans les thématiques) et n'écrit que dans le bon fichier selon le scope. Adapter le prompt si nécessaire.
- [ ] **Connecter `barky.fr`** sur Shopify quand le domaine est prêt (remplacera l'alias `barky-8363.myshopify.com`).
- [ ] **Push GitHub privé du repo** pour activer la migration de la routine cron 17h vers le skill cloud `schedule` (mentionné dans `BARKY_CERVEAU.md §5.8`).
